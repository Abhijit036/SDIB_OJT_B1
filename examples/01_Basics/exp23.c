#include<stdio.h>
int main()
{
    int l,b,A;
    printf("Enter the length of the rectangle=");
    scanf("%d",&l);
    printf("Enter the length of the rectangle=%d",l);
    printf("\nenter the breadth of the rectangle:");
    scanf("%d",&b);
    printf("Enter the length of the rectangle=%d",l);
    A=l*b;
    printf("area of the rectangle:%d",A);
    return 0;
}